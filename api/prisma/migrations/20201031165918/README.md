# Migration `20201031165918`

This migration has been generated by inregist at 10/31/2020, 11:59:18 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql

```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20201031165918
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,82 @@
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator prisma_client_js {
+  provider = "prisma-client-js"
+}
+
+enum EnableStatus {
+  SHOW
+  HIDE
+  DELETE
+}
+
+enum UserRole {
+  CASHIER
+  CHEF
+  NONE
+}
+
+enum TaskStatus {
+  PENDING
+  ONGOING
+  CANCELED
+  TIMEUP
+  COMPLETED
+}
+
+enum SteamerStatus {
+  AVAILABLE
+  UNAVAILABLE
+}
+
+model User {
+  id        String    @default(uuid()) @id
+  createdAt DateTime  @default(now())
+  updatedAt DateTime  @updatedAt
+  name      String?
+  userName  String    @unique
+  password  String
+  enableStatus EnableStatus
+}
+
+model LogUser {
+  id        String    @default(uuid()) @id
+  @@index(userId)
+  user      User      @relation(fields: [userId], references: [id])
+  userId    String
+  createdAt DateTime  @default(now())
+  role      UserRole
+}
+
+model Task {
+  id          String      @default(uuid()) @id
+  name        String
+  total       Int
+  status      TaskStatus
+  createdAt   DateTime    @default(now())
+  updatedAt   DateTime    @updatedAt
+  finishTime  DateTime
+  countTime   Int
+  priority    Int
+  @@index(createdBy)
+  user        User     @relation(fields: [createdBy], references: [id])
+  createdBy   String
+  updatedBy   String
+  Steamer     Steamer[]
+  serverId    String?
+}
+
+model Steamer {
+  id             String           @default(uuid()) @id
+  steamerNo      Int
+  machineNo      Int
+  status         SteamerStatus
+  updatedAt      DateTime         @default(now()) @updatedAt
+  updatedBy      String
+  Task           Task?             @relation(fields: [taskId], references: [id])
+  taskId         String?
+}
+
```


